<%- include('../partials/shell-start', { bodyClass: 'portal-shell' }) %>
<section class="section-sm max-w-3xl">
  <%- include('../partials/portal-nav', { backHref: '/agent/mailbox?folder=' + encodeURIComponent(activeFolder || 'INBOX'), backLabel: 'Mailbox' }) %>
  <div class="mb-8">
    <p class="badge bg-white text-secondary-800 shadow-soft">Mailbox</p>
    <h1 class="mt-3 text-display-md"><%= mode === 'reply' ? 'Reply' : 'Compose' %></h1>
    <p class="mt-2 text-primary-700">Outgoing mail is sent in the Destinations With Deanna branded template (logo, styling and signature).</p>
  </div>

  <% if (error) { %><div class="mb-5 rounded-card border border-danger-100 bg-danger-50 px-4 py-3 text-danger-700" role="alert"><%= error %></div><% } %>

  <form method="post" action="/agent/mailbox/send?_csrf=<%= encodeURIComponent(csrfToken) %>" enctype="multipart/form-data" class="card space-y-4">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <input type="hidden" name="mode" value="<%= mode %>" />
    <input type="hidden" name="folder" value="<%= form.folder || activeFolder || 'INBOX' %>" />
    <input type="hidden" name="inReplyTo" value="<%= form.inReplyTo || '' %>" />
    <input type="hidden" name="references" value="<%= form.references || '' %>" />

    <div>
      <label class="label" for="to">To</label>
      <input class="field" id="to" name="to" type="text" value="<%= form.to || '' %>" placeholder="client@example.com" />
    </div>
    <div>
      <label class="label" for="cc">Cc <span class="font-normal text-primary-500">(optional)</span></label>
      <input class="field" id="cc" name="cc" type="text" value="<%= form.cc || '' %>" />
    </div>
    <div>
      <label class="label" for="subject">Subject</label>
      <input class="field" id="subject" name="subject" type="text" value="<%= form.subject || '' %>" />
    </div>
    <div>
      <label class="label" for="body">Message</label>
      <textarea class="field min-h-[260px]" id="body" name="body"><%= form.body || '' %></textarea>
      <p class="help">Blank line = new paragraph. Links starting with https:// become clickable. Your message is wrapped in the branded email layout with logo and signature when sent.</p>
    </div>
    <div>
      <label class="label" for="attachments">Attachments <span class="font-normal text-primary-500">(optional)</span></label>
      <input class="field" id="attachments" name="attachments" type="file" multiple />
      <p class="help">Up to 5 files, 10MB each. Allowed: PDF, images, Office (Word/Excel/PowerPoint), text/CSV, and zip. Files are sent with the email only — not stored on the server. Drafts ignore attachments.</p>
    </div>

    <div class="flex flex-wrap gap-2 pt-2">
      <button class="btn-primary" type="submit" name="action" value="send">Send email</button>
      <button class="btn-secondary" type="submit" name="action" value="draft">Save draft</button>
      <a href="/agent/mailbox?folder=<%= encodeURIComponent(activeFolder || 'INBOX') %>" class="btn-ghost">Cancel</a>
    </div>
  </form>
</section>
<%- include('../partials/shell-end') %>
